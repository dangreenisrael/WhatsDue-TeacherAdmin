<div class="modal-header">
    <button type="button" {{action 'close'}} class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h4 class="modal-title">{{course_name}}</h4>
</div>
<div class="modal-body">
    <form action="#" class="form-horizontal ">
        <div class="form-group">
            <div class="col-md-12">
                {{input type="text" value=course_name maxlength="20" class="form-control" size="16" placeholder="Course Name"}}
            </div>
        </div>

    </form>
</div>
<div class="modal-footer">
    <button  data-dismiss="modal" class="btn btn-primary" type="button" {{action 'save'}} >Save</button>
    <button  class="btn btn-danger pull-left" type="button" {{action 'remove'}} >Delete</button>
</div>